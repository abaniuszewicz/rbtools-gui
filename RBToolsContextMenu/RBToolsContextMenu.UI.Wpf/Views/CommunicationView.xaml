<Page x:Class="RBToolsContextMenu.UI.Wpf.Views.CommunicationView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:RBToolsContextMenu.UI.Wpf.Views"
      xmlns:ui="http://schemas.modernwpf.com/2019"
      xmlns:viewmodels="clr-namespace:RBToolsContextMenu.UI.Wpf.ViewModels"
      mc:Ignorable="d"
      KeepAlive="True"
      Title="CommunicationView">
    <Page.DataContext>
        <viewmodels:CommunicationViewModel/>
    </Page.DataContext>
    
    <Grid>
        <TextBox ui:ControlHelper.Header="Communication"
                 ui:ControlHelper.PlaceholderText="This is where sent/received command will appear"
                 Text="{Binding Issuer.CommandStack, UpdateSourceTrigger=PropertyChanged, Mode=OneWay}"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 ScrollViewer.HorizontalScrollBarVisibility="Auto"/>
    </Grid>
</Page>
